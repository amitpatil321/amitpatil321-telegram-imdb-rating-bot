{
  "name": "movie-rating-telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot which returns movie info along with IMDB rating",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env index.js",
    "watch": "nodemon --env-file=.env index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "semantic-release"
  },
  "author": "Amit",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "express": "^4.21.1",
    "node-telegram-bot-api": "^0.66.0"
  },
  "devDependencies": {
    "standard-version": "^9.5.0"
  }
}
